"""Minimal setup.py shim for editable installs.

This file exists only to support editable installs with older pip versions.
All configuration is in pyproject.toml following PEP 517/518.
"""

from setuptools import setup

setup()
